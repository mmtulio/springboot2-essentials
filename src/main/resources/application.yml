
server:
  error:
    include-stacktrace: on_param

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/anime?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG

management:
  endpoint:
    web:
      exposure:
        include: "*"

info:
  app: Spring Boot 2 Essentials
  website: https://www.linkedin.com/in/tuliomuller/
  github: https://github.com/mmtulio/springboot2-essentials